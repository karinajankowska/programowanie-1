#include <stdio.h>

int sumaElementow(int tab[], int size){
    int i;
    int sumaElementow = 0;

    for(i = 0; i < size; i++){
       sumaElementow = sumaElementow + tab[i];
    }
    return sumaElementow;
}

int main()
{
    int tab[5];
    int i,
    suma = 0;
    
    printf ("Wpisz elementy tablicy:\n");
    int size = sizeof(tab)/sizeof(int);
    for (i=0 ; i < size; i++){
    scanf("%d", &tab[i]);
        suma = sumaElementow + tab[i];
    }

    printf("Średnia1 to: %d\n",sumaElementow(tab,i));
    printf("Średnia2 to: %d\n",sumaElementow(tab,size));
    
  return 0;
}
