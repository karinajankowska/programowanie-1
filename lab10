#include <stdio.h>

int
main ()
{
  int tab[5];
  int i;
  int rozmiarTab;
  rozmiarTab = sizeof (tab) / sizeof (int);
  for (i = 0; i < 5; i++)
    {
      scanf ("%d", &tab[i]);
    }

  for (i = 4; i > -1; i--)
    {
      printf ("Element %d to %d\n", i, tab[i]);
    }
  return 0;
}
