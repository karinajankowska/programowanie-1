#include <stdio.h>

int main()
{
int tab[5], min, max, i;
for(i=0; i<5; i++){
    scanf("%d", &tab[i]);
}
min = tab[0];
max = tab[0];
for(i=1; i<5; i++){
    if(tab[i] < min){
        min = tab[i];
    }

    if(tab[i] > max){
        max = tab[i];
    }
}
printf("Najmniejszym elementem w tablicy jest: %d\n", min);
printf("Największym elementem w tablicy jest: %d\n", max);
    return 0;
}
